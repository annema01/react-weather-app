import React from "react";
// setup a switch case for the image (or the data maping que le prof a fait)
export default function CurrentWeatherImg() {
  return <div className="CurrentWeatherImg">Background image</div>;
}
